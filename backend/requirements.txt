# Полный набор для локальной разработки (api + worker в одном окружении).
# Прод-образы собираются из requirements-api.txt / requirements-worker.txt.

-r requirements-api.txt
-r requirements-worker.txt
